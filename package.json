{
  "name": "agent-runtime",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web",
    "packages/agent-ui-contracts"
  ],
  "scripts": {
    "web:dev": "bun --filter agent-web run dev",
    "web:build": "bun --filter agent-web run build",
    "web:typecheck": "bun --filter agent-web run typecheck",
    "web:preview": "bun --filter agent-web run preview"
  },
  "packageManager": "bun@latest"
}
